function xrms = mean_square(x,x_0,N)

for i=1:N
    for k=1:3
            if x(i,k) - x_0(j,k)<
            
            
            
            rij(1,k) = periodic(rij(1,k),L(1,k));   %periodic boundaries
        xrms(i,1) = sqrt( (x(i,1)-x_0(i,1))^2 + (x(i,2)-x_0(i,2))^2 + (x(i,3)-x_0(i,3))^2);
end

xrms = mean(xrms);



